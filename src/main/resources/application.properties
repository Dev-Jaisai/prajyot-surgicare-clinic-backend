spring.application.name=clinic

# ==========================================
# ? OLD MYSQL CONFIGURATION (COMMENTED)
# ==========================================
# spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/clinic_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:test}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# ? NEW POSTGRESQL CONFIGURATION (RAILWAY + NEON)
# ==========================================
# Railway ?? DB_URL, DB_USER, DB_PASSWORD ?? Variables ??? ???? ??? ????? ????
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ---------- HIKARI ----------
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# ---------- JPA ----------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# ---------- LOGGING ----------
logging.level.com.prajyotsurgicare.clinic=INFO
logging.level.org.springframework=INFO

# ---------- SERVER ----------
server.port=8080
server.address=0.0.0.0

# ---------- ACTUATOR ----------
management.endpoints.web.exposure.include=health,info

# ---------- JSON ----------
spring.jackson.time-zone=UTC
# Increase file upload size
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Secrets moved to Environment Variables
twilio.account-sid=${TWILIO_ACCOUNT_SID}
twilio.auth-token=${TWILIO_AUTH_TOKEN}
twilio.phone-number=+17179708922